[build]
  publish = "client/dist"
  command = "pnpm install && cd client && npx expo export --platform web"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
